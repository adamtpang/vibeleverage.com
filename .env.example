# Anthropic API key for the "Talk to Vibe Coach" chat agent (server-side only).
# Used by app/api/chat/route.ts. Get one at https://console.anthropic.com.
# Local dev: copy this file to .env.local and fill it in. Never commit a real key.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
